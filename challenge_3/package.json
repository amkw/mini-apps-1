{
  "name": "challenge_3",
  "version": "1.0.0",
  "description": "single-page app that takes a user through a series of forms that simulate a shopping cart checkout experience",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon server.js babel ./node_modules/.bin/babel client --out-dir public --presets @babel/preset-react --source-maps inline . --watch"
  },
  "author": "Alyssa Wei",
  "license": "UNLICENSED",
  "dependencies": {
    "@babel/cli": "^7.4.4",
    "@babel/core": "^7.4.4",
    "@babel/preset-react": "^7.0.0",
    "express": "^4.16.4",
    "nodemon": "^1.19.0"
  }
}
